<% if @vote.valid? %>
  $('#post_<%= @post.id %>_votes').html('<%= @post.total_votes %> votes') // $('#post_1_votes').html('2 votes')
<% else %>
  alert('You can only vote on a post once.');
<% end %>